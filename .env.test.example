# Test Configuration File
# Copy this file to .env.test and fill in your test credentials
# 
# WARNING: This file contains test credentials for development/testing purposes ONLY.
# These credentials should NEVER be used in production environments.
# 
# If these credentials were ever exposed in version control, they MUST be rotated immediately.
#
# Usage:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Fill in your test account credentials below
# 3. The .env.test file is gitignored and will not be committed to version control

# Test account username/email (for development/testing only)
TEST_DEFAULT_USERNAME=your.test.email@example.com

# Test account password (for development/testing only)
TEST_DEFAULT_PASSWORD=your_test_password_here

# Test host (default: localhost:8080)
TEST_DEFAULT_HOST=localhost:8080
